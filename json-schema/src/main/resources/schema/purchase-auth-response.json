{
  "$schema": "http://json-schema.org/draft-07/schema#",

  "type": "object",
  "title": "PurchaseAuthResponse",
  "description": "Response to PurchaseAuthRequest",

  "properties": {
    "result": { "$ref": "#/definitions/code" },
    "approvalCode": { "$ref": "model.json#/approvalcode" }
  },

  "definitions": {
    "code": {
      "type": "string",
      "enum": ["OK", "WRONG"],
      "description": "Result of the request"
    }
  }
}
